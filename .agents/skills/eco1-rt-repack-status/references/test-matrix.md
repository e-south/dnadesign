# Test Matrix

| Scenario | Prompt | Expected Behavior | Pass/Fail |
| --- | --- | --- | --- |
| Trigger positive | Where is eco1_rt_repack now? | Use the checked-in study record and report record-only status. | Pass if the answer reports phase, blockers, and next route without inventing an OPS provider. |
| Trigger negative | Where is rt_lnrna_sponging_construct_triage now? | Route away from this study-specific skill. | Pass if the skill does not generalize Eco1 RT repack status to the RT-lnRNA study. |
| Contract boundary | Is `thread` implemented? | Say no; route to the dev spec and fixture scaffold. | Pass if the answer distinguishes planned contracts from source code. |
| Policy routing | Which residues are fixed? | Route to residue-mask policy and conservative-mask fixture. | Pass if catalytic/contact/conservation masks stay study-owned. |
| Gate routing | What blocks MPNN request generation? | Route to structure authority, mask contract, and sampling plan gates. | Pass if the answer does not jump to backend execution. |
| Conservation routing | What blocks conservation masking? | Route to the MSA method, conservation source contract, provider-source materializer, roster-cache materializer, source-sequence materializer plus sufficiency gate, conservation-alignment materializer, `aligner.msa` for generic aligned FASTA bundles, `aligner.msa.visualization` for generic QC sidecars, Eco1-owned annotation tracks, Eco1-owned exemplar rows, Eco1-owned panel specs, and the conservation materializer. | Pass if the answer says `ec86_clade9_conservation_v1` is the selected Mestre Ec86 RT clade 9 conservation profile, Tao is the plurality/frequency masking method, the full Mestre roster is candidate/context only, clade 9 source records and source FASTA sufficiency now pass locally after declared QC, the hardened `aligner.msa` seam publishes aligned FASTA atomically and records stderr, Clustal Omega is the current selected backend, the previous Eco1-family alignment/visualization is historical local evidence until regenerated, Eco1 motif annotations, exemplar-row selections, and panel-display specs remain study-owned, the former full-roster broad MAFFT run was stopped after roughly four hours, the complete accepted aligned FASTA bundle and conservation profile are still missing, and source authority, provider ledgers, conservation scoring, or mask policy are not moved into aligner. |
| Handoff routing | What blocks RT-lnRNA use of these candidates? | Route to the downstream handoff readiness check. | Pass if paired construct semantics stay downstream. |
| Repeatability | Run the skill audit twice. | Structural and routing checks remain deterministic. | Pass if both runs finish with no failures and no generated outputs are required. |
| Source/test IA regression | Add a flat operation module, flat primitive materializer, flat materialization test, root-level source-sequence subprimitive test, or root-level MSA visualization implementation file. | Audit fails before the status skill can be treated as healthy. | Pass if study code stays under semantic `operations/contracts/`, `operations/materialization/<primitive>/`, `tests/contracts/`, and `tests/materialization/<primitive>/` packages, source-sequence subprimitives mirror under `tests/materialization/source_sequences/<subprimitive>/`, and generic visualization implementation stays under `aligner.msa.visualization.contracts`, `aligner.msa.visualization.materialization`, or `aligner.msa.visualization.renderers`. |
