# Test Matrix

| Scenario | Prompt | Expected Behavior | Pass/Fail |
| --- | --- | --- | --- |
| Trigger positive | Where is eco1_rt_repack now? | Use the checked-in study record and report record-only status. | Pass if the answer reports phase, blockers, and next route without inventing an OPS provider. |
| Trigger negative | Where is rt_lnrna_sponging_construct_triage now? | Route away from this study-specific skill. | Pass if the skill does not generalize Eco1 RT repack status to the RT-lnRNA study. |
| Contract boundary | Is `thread` implemented? | Say that generic ProteinMPNN request, sample-ingest, candidate-table, ColabFold output normalization, ESM Atlas sparse-activation normalization, Biohub ESMC query-time SAE normalization, structure-prediction registry, and fold-check request/report contracts are implemented; route broader thread questions to the dev spec. Eco1 fold-check review remains study-owned. | Pass if the answer distinguishes implemented request/sample/candidate/foldcheck/Atlas/Biohub ESMC/structure-registry mechanics and study-owned review from planned fold-model execution, feasibility, and handoff contracts. |
| Policy routing | Which residues are fixed? | Route to residue-mask policy, manual mask-authority ontology, `operations/masking/`, `operations/contracts/masks/`, generated manual mask-authority artifact, and contact/conservation evidence artifacts. | Pass if the answer reports `eco1_rt_clade9_plurality25_direct_contact5a_v1`: protected residues are NAxxH/YADD/VTG, Wang/Ec86 direct substrate-contact priors, Ec86 clade 9 >=25% WT-plurality conservation calls, or mapped residues within 5 A retained DNA/RNA; RT1-RT7 intervals are review labels, not blanket hard masks; terminal residues 1, 2, and 312-320 are `non_fixed_missing_backbone`. |
| Gate routing | What blocks MPNN request generation? | Route to structure authority, mask contract, the current mask rule, sampling plan gates, thread-plan materialization, ProteinMPNN request adaptation, sample ingest, candidate-table construction, fold-check request generation, fold-check report normalization, fold-check review, review deliverables, Atlas lookup, Biohub ESMC SAE profile, and WT ESMC mutation scoring. | Pass if the answer says `thread_plan.yaml`, `proteinmpnn_request/request_manifest.yaml`, `sample_table.parquet`, `candidate_table.parquet`, `foldcheck_request/foldcheck_request_manifest.yaml`, `foldcheck_report.parquet`, `foldcheck_review/`, `review_deliverables/`, `biohub_esmc_sae_profile.parquet`, and WT ESMC mutation scoring are materialized under `eco1_rt_clade9_plurality25_direct_contact5a_v1`; the next blockers are structure review, feasibility, and handoff. |
| Conservation routing | What blocks conservation masking? | Route to the MSA method, conservation source contract, provider-source materializer, roster-cache materializer, source-sequence materializer plus sufficiency gate, conservation-alignment materializer, `aligner.msa` for generic aligned FASTA bundles, `aligner.msa.visualization` for generic QC sidecars, Eco1-owned annotation tracks, Eco1-owned exemplar rows, Eco1-owned panel specs, the conservation materializer, manual mask-authority materializer, and the mask-set materializer. | Pass if the answer says `ec86_clade9_conservation_v1` is the selected Mestre Ec86 RT clade 9 conservation profile used by the mask, Tao is the plurality/frequency masking method, the full Mestre roster is candidate/context only, clade 9 source records and source FASTA sufficiency pass locally after declared QC, Clustal Omega is the current selected backend, both selected aligned FASTA bundles and `conservation_profile.parquet` are materialized locally, manual mask authority is materialized for NAxxH/YADD/VTG anchors, RT1-RT7 review labels, and Wang/Ec86 direct-contact mask priors, `mask_set.yaml` is materialized under `eco1_rt_clade9_plurality25_direct_contact5a_v1`, and source authority, provider ledgers, conservation scoring, or mask policy are not moved into aligner. |
| Handoff routing | What blocks RT-lnRNA use of these candidates? | Route to the downstream handoff readiness check. | Pass if paired construct semantics stay downstream. |
| Repeatability | Run the skill audit twice. | Structural and routing checks remain deterministic. | Pass if both runs finish with no failures and no generated outputs are required. |
| Source/test IA regression | Add a flat operation module, flat primitive materializer, flat materialization test, root-level conservation/contact-risk/structure/mask contract file, root-level source-sequence subprimitive test, or root-level MSA visualization implementation file. | Audit fails before the status skill can be treated as healthy. | Pass if study code keeps shared contract orchestration under `operations/contracts/`, domain validators under `operations/contracts/conservation/`, `operations/contracts/contact_risk/`, `operations/contracts/masks/`, and `operations/contracts/structure/`, RT interval validation under `operations/contracts/masks/rt_intervals.py`, materializers under `operations/materialization/<primitive>/`, contact-geometry mmCIF parsing under `contact_geometry/structure_io.py`, contact-geometry row math under `contact_geometry/rows.py`, contact-geometry Parquet writing under `contact_geometry/writer.py`, mirrored tests under `tests/contracts/<domain>/` and `tests/materialization/<primitive>/`, source-sequence subprimitives under `tests/materialization/source_sequences/<subprimitive>/`, and generic visualization implementation under `aligner.msa.visualization.contracts`, `aligner.msa.visualization.materialization`, or `aligner.msa.visualization.renderers`. |
