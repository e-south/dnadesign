Archived DenseGen runs and legacy configs live here.

- These directories are kept out of the active run list.
- Some legacy configs may not validate against the current schema (by design).
- To inspect archived runs:
  - dense ls-runs --root runs/_archive
