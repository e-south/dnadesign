## Study Operations

This directory contains study-owned operational code.

- `status/`: OPS status and preflight provider implementation. It reads the
  docs-side study record and exposes cheap status plus command-backed readiness
  checks.

Do not put OPAL decision experiments or durable review prose here.
