# Atlas 2x2 Intermediate Main

This deliverable is the primary four-panel latent atlas for the promoter study.
It compares 7B vs 20B and short-context vs anchor-context intermediate embeddings
for the same promoter families. The figure is expected to keep the wild-type and
reference promoter set visible in every panel so cross-panel comparisons do not
depend on memory or off-plot lookup.
