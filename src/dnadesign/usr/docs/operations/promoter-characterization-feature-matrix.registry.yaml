schema_version: 1
catalog_order: 60
registry_id: usr.data-plane.promoter-feature-matrix
type: runbook
plane: data-plane
owner_boundary: usr
entry_artifact: one or more USR-backed promoter datasets plus optional construct-expanded context datasets
exit_artifact: infer-annotated USR feature matrix ready for cluster or OPAL
summary: Build one infer-annotated feature matrix from mixed promoter sources before branching into Cluster or OPAL.
execution_kind: staged
progress_kind: usr-dataset-state
related_tools:
  - densegen
  - construct
  - infer
  - cluster
  - opal
related_tool_routes:
  - tool: construct
    route: template-contexts
  - tool: infer
    route: architecture
  - tool: infer
    route: evo2-provider
  - tool: infer
    route: evo2-promoter-features
  - tool: cluster
    route: exploratory-clustering
  - tool: opal
    route: usr-infer-x-active-learning
relations:
  - type: depends-on
    target: usr.data-plane.multi-source-source-of-truth
  - type: depends-on
    target: usr.data-plane.construct-infer-source-of-truth
  - type: handoff-to
    target: cluster.downstream.exploratory-clustering
  - type: handoff-to
    target: opal.downstream.usr-infer-x-active-learning
