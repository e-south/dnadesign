# cluster/jobs/promoter_clusters_v1/analyze.yaml
command: "analyze"
params:
  dataset: "60bp_dual_promoter_cpxR_LexA"
  usr_root: "../../../usr"
  cluster_col: "cluster__promoter_clusters_v1"
  preset: "analysis.promoter_set1"

  # Optional override (otherwise: results/<FIT>/analysis/)
  # out_dir: "results/promoter_clusters_v1/analysis"
