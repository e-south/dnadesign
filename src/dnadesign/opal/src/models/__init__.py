"""
--------------------------------------------------------------------------------
<dnadesign project>
src/dnadesign/opal/src/models/__init__.py

Model registry package.

Holds top-layer model wrappers with a simple, uniform interface:
- fit(X, y) -> metrics,
- predict(X) -> Ŷ,
- feature_importances(),
- save(path) / load(path).

Module Author(s): Eric J. South
Dunlop Lab
--------------------------------------------------------------------------------
"""
