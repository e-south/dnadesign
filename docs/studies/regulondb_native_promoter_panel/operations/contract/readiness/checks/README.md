## RegulonDB Readiness Checks

These fragments merge into `ops.study.yaml` `parts.preflight.checks`.

Each file is named after the lifecycle phase it checks. This remains a
providerless record contract until `operations/ops.study.yaml` declares
concrete `ops_surfaces`.
