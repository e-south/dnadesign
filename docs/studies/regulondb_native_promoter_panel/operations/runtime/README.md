## RegulonDB Runtime Context

`pipeline.yaml` stores command groups and downstream surface bindings for the
RegulonDB native promoter panel. It is supplemental runtime context; the
OPS-facing contract is `../ops.study.yaml` plus `../contract/`.
