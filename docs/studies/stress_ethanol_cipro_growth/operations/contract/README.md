## Stress Ops Contract Parts

These YAML files are loaded by `../ops.study.yaml`. Keep this directory for
OPS-facing lifecycle, artifact, execution-surface, snapshot, and preflight
declarations. Put command-group narrative and downstream binding context in
`../runtime/pipeline.yaml`.
