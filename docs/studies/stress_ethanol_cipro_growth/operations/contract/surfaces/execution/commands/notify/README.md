## Stress Notify Command Surfaces

Notify command fragments are split by subcommand family because each model lane
has both a profile-doctor and event-resolution surface.

- `profile-doctor.yaml`: `notify profile doctor` checks for Infer profiles.
- `resolve-events.yaml`: `notify setup resolve-events` checks for Infer configs.
