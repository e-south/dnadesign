mode: sequential
phase_order:
  - phase0_scaffold
  - phase1_thread_contract
  - phase2_real_backend_ingest
  - phase3_foldcheck_report
  - phase4_downstream_promotion
current_phase:
  strategy: explicit
  id: phase3_foldcheck_report
provider_registered: false
notes:
  - No OPS provider is registered for this study yet.
  - Use checked-in records and routes for status until executable providers exist.
  - Selection readiness is materialized, but RT-only candidate_handoff.yaml remains a downstream blocker.
