## Retron Readiness Checks

These YAML fragments merge into `ops.study.yaml` `parts.preflight`.

Each file is named after the Retron track it checks. Keep cross-track scope,
group bindings, and next-scope rules in the sibling `scope.yaml`,
`group-bindings.yaml`, and `next-scope.yaml` files.
