## Retron Ops Contract Parts

These YAML fragments are loaded by `../ops.study.yaml`.

- `lifecycle/`: lifecycle mode and track order.
- `surfaces/`: artifacts and execution-surface refs.
- `status/`: snapshot scope.
- `readiness/`: preflight groups and checks.

Put compiler command groups and native-agent bootstrap context in
`../runtime/command-groups/pipeline.yaml`.
