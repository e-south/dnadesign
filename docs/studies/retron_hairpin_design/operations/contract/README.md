## Retron Ops Contract Parts

These YAML files are loaded by `../ops.study.yaml`. Keep lifecycle tracks,
artifact refs, execution surfaces, snapshot scope, and preflight checks here.
Put compiler command groups and native-agent bootstrap context in
`../runtime/pipeline.yaml`.
